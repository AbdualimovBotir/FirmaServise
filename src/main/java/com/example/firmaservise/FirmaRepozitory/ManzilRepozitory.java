package com.example.firmaservise.FirmaRepozitory;

import com.example.firmaservise.Entity.ManzilEntity;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ManzilRepozitory extends JpaRepository<ManzilEntity,Integer> {
}
